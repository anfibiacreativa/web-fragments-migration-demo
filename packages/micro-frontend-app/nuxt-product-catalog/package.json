{
  "name": "nuxt-product-catalog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "NITRO_PORT=4001 HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "NITRO_PORT=4175 nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy:pages": "npm run build && wrangler pages deploy"
  },
  "dependencies": {
    "nuxt": "^3.15.4",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.10.0",
    "@cloudflare/workers-types": "^4.20250414.0",
		"nitro-cloudflare-dev": "^0.2.2"
  }
}
